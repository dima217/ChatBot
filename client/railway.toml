# Railway: set this service Root Directory to `client`, then deploy.
# Set NEXT_PUBLIC_API_URL to your API service public URL before build (Variables tab).

[build]
builder = "nixpacks"

[deploy]
# Monorepo root: workspace start for Next.js.
startCommand = "npm run start:client"
healthcheckPath = "/"
healthcheckTimeout = 60
